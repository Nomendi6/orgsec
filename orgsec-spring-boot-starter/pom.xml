<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.nomendi6.orgsec</groupId>
        <artifactId>orgsec-parent</artifactId>
        <version>2.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>orgsec-spring-boot-starter</artifactId>
    <name>OrgSec Spring Boot Starter</name>
    <description>Spring Boot Starter for OrgSec Security Library</description>

    <dependencies>
        <!-- OrgSec Core -->
        <dependency>
            <groupId>com.nomendi6.orgsec</groupId>
            <artifactId>orgsec-core</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- OrgSec Common -->
        <dependency>
            <groupId>com.nomendi6.orgsec</groupId>
            <artifactId>orgsec-common</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Spring Boot -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
        </dependency>

        <!-- Spring Security -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- Optional at runtime: enabling the Person API requires an application JwtDecoder and
             the resource-server filter that authenticates its Bearer token. JWT storage brings
             this transitively; non-JWT applications are not forced to do so. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Spring Web (for Person API) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Default InMemory storage
             NOTE: While this is a compile-time dependency (application code uses InMemory classes),
             at runtime you can override with Redis storage using @Primary annotation.
             InMemory beans will not be created when Redis is active (via @ConditionalOnMissingBean).
        -->
        <dependency>
            <groupId>com.nomendi6.orgsec</groupId>
            <artifactId>orgsec-storage-inmemory</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Storage modules pulled in only for PropertiesDocumentationCoverageTest -->
        <dependency>
            <groupId>com.nomendi6.orgsec</groupId>
            <artifactId>orgsec-storage-redis</artifactId>
            <version>${project.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.nomendi6.orgsec</groupId>
            <artifactId>orgsec-storage-jwt</artifactId>
            <version>${project.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
