/**
 * Loader classes for parsing query results into model objects.
 *
 * <p>Contains:</p>
 * <ul>
 *   <li>{@code PersonLoader} - Loads PersonDef objects from query results</li>
 *   <li>{@code OrganizationLoader} - Loads OrganizationDef objects from query results</li>
 *   <li>{@code RoleLoader} - Loads RoleDef objects from query results</li>
 *   <li>{@code PrivilegeLoader} - Initializes privilege definitions</li>
 * </ul>
 *
 * @since 1.0.0
 */
package com.nomendi6.orgsec.common.loader;
